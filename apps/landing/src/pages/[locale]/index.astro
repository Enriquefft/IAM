---
import Base from "@/layouts/Base.astro";
import Hero from "@/components/sections/Hero.astro";
import Backers from "@/components/sections/Backers.astro";
import Features from "@/components/sections/Features.astro";
import Pricing from "@/components/sections/Pricing.astro";
import Equipo from "@/components/sections/Equipo.astro";
import Faq from "@/components/sections/Faq.astro";
import FinalCta from "@/components/sections/FinalCta.astro";
import Waitlist from "@/components/sections/Waitlist.astro";
import Footer from "@/components/sections/Footer.astro";
import { isLocalePath, type LocalePath } from "@/lib/i18n";

// Locale comes from Astro's i18n machinery (configured in astro.config.mjs).
// Type-narrow with isLocalePath; middleware guarantees prefix presence.
const current = Astro.currentLocale;
if (!isLocalePath(current)) return Astro.redirect("/", 302);
const locale: LocalePath = current;
---

<Base pageKey="home" locale={locale}>
  <Hero locale={locale} />
  <Backers />
  <Features locale={locale} />
  <Pricing locale={locale} />
  <Equipo locale={locale} />
  <Faq locale={locale} />
  <FinalCta locale={locale} />
  <Waitlist locale={locale} />
  <Footer slot="footer" locale={locale} />
</Base>
